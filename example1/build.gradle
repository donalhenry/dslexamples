import com.donalhenry.gradle.example1.*

apply plugin: CookingDslPlugin

devconfig {
  teams {
    elections {
    }
    engineering {
      size = 4
    }
    pfunk {
    }
    player {
      size = 2
    }
  }
}

task listAllTeams << {
  project.devconfig.teams.each { team ->
    println "Team $team.name has $team.sizeString."
  }
}
